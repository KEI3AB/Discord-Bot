#ifndef CURRENCY_RATE_PROCESSOR
#define CURRENCY_RATE_PROCESSOR

#include <string>

class CurrencyRateProcessor {
public:
    void process(const std::string& response);
};


#endif // CURRENCY_RATE_PROCESSOR